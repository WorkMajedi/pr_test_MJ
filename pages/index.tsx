import React from "react";

import { HomeScreen } from "../src/screens";

const Home: React.FC = (props) => {
    return (
        <>
            <HomeScreen {...props} />
        </>
    );
};
export default Home;
